apiVersion: v1
kind: Service
metadata:
  name: os-storage-service
  labels:
    app: os-storage-service
spec:
  ports:
    - name: http
      port: 80
      targetPort: 8080
  selector:
    app: os-storage-service
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: os-storage-service
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: os-storage-service
        version: v1
    spec:
      containers:
        - name: os-storage-app
          envFrom:
            - configMapRef:
                name: os-service-config
          image: gcr.io/opendes/os-storage-app
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
