# Default values for storage-configmap.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

data:
  entitlements_host: "http://entitlements"
  partition_host: "http://partition"
  legaltag_host: "http://legal"
  crs_converter_host: "http://crs-converter"
  log_level: "INFO"
  google_audiences: ""
  redis_group_host: ""
  redis_storage_host: ""
  default_data_country: "US"

conf:
  configmap: "storage-config"
  app_name: "storage"
